interface:
  display_name: "Dummy Boss"
  short_description: "Coordinate subagents across supported model providers"
  default_prompt: "Use $dummy-boss to delegate this task to Astra Low subagents while you coordinate and report the verified result."
